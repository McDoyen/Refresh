import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
  container: {},
  form: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    marginTop: "25%",
  },
}));
